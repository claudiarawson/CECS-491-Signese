# Data Directory

Local-only data lives here and is mostly ignored by git.

- `data/raw/`: downloaded source datasets
- `data/processed/`: generated subsets, tensors, and model-ready exports

Keep manifests/config in source-controlled code (for example `src/features/translate/model/dataset.manifest.json`).
